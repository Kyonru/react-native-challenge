import React from 'react';
import Icon from 'react-native-vector-icons/Fontisto';

import colors from 'src/theme/colors';

export default () => <Icon name="cocktail" size={100} color={colors.white} />;
